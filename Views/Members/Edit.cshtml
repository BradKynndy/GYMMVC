@model GYMMVC.Models.MembersViewModel

@{
    ViewBang.Title = "Editar Membro";
}
<h2>Editar dados do Membro</h2>
@using (Html.BaginForm())
{
    @Html.AntigorgeryToken()
    <div class="form-horizontal">
        <hr />
        <div class="form-group">
@Html.LabelFor(model => model.id, htmlAttributes: new { @class = "control-label col-md-2"})
            <div class="col-md-6">
@Html.LabelFor(model => model.id, new { htmlAttibutes = new { @class = "from-control", @readonly = "readonly"} })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Nome, htmlAttributes: new { @class = "control-label col-md-2"})
            <div class="col-md-6">
                @Html.DropDownListFor(model => model.class , Html.GetEnumSelectList(typeof(GYMMVC.Models.Euns.classEnum)),
                    "---selecione---", new {@class = "form-control"})
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-md-6">
                <input type="submit" value="Salvar alterações" class="btn-btn=primary">
            </div>
        </div>
    </div>
}
<div>
    @Html.ActionLink("Retornar", "Index")
</div>